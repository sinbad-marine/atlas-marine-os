import { createClient } from 'https://esm.sh/@supabase/supabase-js@2'
const cors={'Access-Control-Allow-Origin':'*','Access-Control-Allow-Headers':'authorization, x-client-info, apikey, content-type'}
Deno.serve(async request=>{if(request.method==='OPTIONS')return new Response('ok',{headers:cors});try{const body=await request.json(),workspaceId=String(body.workspaceId||''),proposalId=String(body.proposalId||''),decision=String(body.decision||''),note=String(body.note||'').slice(0,2000);if(!workspaceId||!proposalId||!['approved','rejected'].includes(decision))throw new Error('Valid workspace, proposal and decision are required.');const url=Deno.env.get('SUPABASE_URL')!,anon=Deno.env.get('SUPABASE_ANON_KEY')!,service=Deno.env.get('SUPABASE_SERVICE_ROLE_KEY')!,auth=request.headers.get('Authorization')||'',userClient=createClient(url,anon,{global:{headers:{Authorization:auth}}}),admin=createClient(url,service,{auth:{persistSession:false}}),{data:{user},error:userError}=await userClient.auth.getUser();if(userError||!user)throw new Error('Signed-in session required.');const {data:member}=await admin.from('workspace_members').select('role,is_active').eq('workspace_id',workspaceId).eq('user_id',user.id).maybeSingle();if(!member?.is_active||member.role!=='owner')throw new Error('Active Owner role required.');const status=decision==='approved'?'owner_approved':'rejected',{error}=await admin.from('design_change_proposals').update({status,owner_note:note||null,owner_reviewed_by:user.id,owner_reviewed_at:new Date().toISOString(),updated_at:new Date().toISOString()}).eq('id',proposalId).eq('workspace_id',workspaceId).eq('status','submitted');if(error)throw error;return Response.json({ok:true,published:false,nextGate:decision==='approved'?'PROTECTED_IMPLEMENTATION_PR':'NONE'},{headers:{...cors,'Content-Type':'application/json'}})}catch(error){return Response.json({error:error instanceof Error?error.message:String(error)},{status:400,headers:{...cors,'Content-Type':'application/json'}})}})
