'use strict';
const KEY=/^[a-f0-9]{64}$/u,REASONS=new Set(['PROCESS_CRASH','SETTLEMENT_AMBIGUOUS','LEASE_EXPIRED']);
const RECOVERY_VERSION='sinbad-terminal-recovery-observability/3C-v1';
function report(status,reasonCode,claims=[],oldestAgeMs=null){return Object.freeze({version:RECOVERY_VERSION,status,reasonCode,expiredCount:claims.length,oldestAgeMs:Number.isInteger(oldestAgeMs)&&oldestAgeMs>=0?oldestAgeMs:null,claims:Object.freeze(claims)});}
function claim(row){if(!row||typeof row!=='object')return null;const raw=Object.create(null);for(const name of ['claim_key','claimed_at','lease_expires_at']){let descriptor;try{descriptor=Object.getOwnPropertyDescriptor(row,name);}catch{return null;}if(!descriptor||!Object.hasOwn(descriptor,'value')||typeof descriptor.value!=='string')return null;raw[name]=descriptor.value;}if(!KEY.test(raw.claim_key)||!Number.isFinite(Date.parse(raw.claimed_at))||!Number.isFinite(Date.parse(raw.lease_expires_at)))return null;return Object.freeze({claimKey:raw.claim_key,claimedAt:raw.claimed_at,leaseExpiresAt:raw.lease_expires_at});}
function inspectPolicy(input){if(!input||typeof input!=='object')return null;const output=Object.create(null);for(const [name,fallback] of [['limit',100],['slaMs',undefined],['now',undefined]]){let descriptor;try{descriptor=Object.getOwnPropertyDescriptor(input,name);}catch{return null;}if(descriptor!==undefined&&!Object.hasOwn(descriptor,'value'))return null;output[name]=descriptor===undefined?fallback:descriptor.value;}if(!Number.isSafeInteger(output.limit)||output.limit<1||output.limit>500||!Number.isSafeInteger(output.slaMs)||output.slaMs<1000||output.slaMs>604800000||(output.now!==undefined&&typeof output.now!=='function'))return null;return Object.freeze(output);}
function create(options={}){
  if(!options.client||typeof options.client.rpc!=='function'||options.serviceRole!==true)throw new TypeError('A trusted server-side Supabase service-role client is required');
  const actorHash=String(options.actorHash||'');if(!KEY.test(actorHash))throw new TypeError('A SHA-256 operator identity is required');
  const client=options.client;
  async function verify(){const {data,error}=await client.rpc('verify_terminal_recovery_access',{});return !error&&data===true;}
  return Object.freeze({
    healthCheck:verify,
    async listExpired(limit=100){if(!Number.isSafeInteger(limit)||limit<1||limit>500||!await verify())return Object.freeze([]);const {data,error}=await client.rpc('list_expired_terminal_delivery_claims',{p_limit:limit});if(error||!Array.isArray(data))return Object.freeze([]);const claims=Object.freeze(data.map(claim).filter(Boolean));return claims.length===data.length?claims:Object.freeze([]);},
    async inspect(input={}){const policy=inspectPolicy(input);if(!policy)return report('RECOVERY_UNAVAILABLE','RECOVERY_INVALID_INSPECT_ARGS');const {limit,slaMs}=policy;let now;try{now=policy.now===undefined?Date.now():policy.now();}catch{return report('RECOVERY_UNAVAILABLE','RECOVERY_INVALID_INSPECT_ARGS');}if(!Number.isSafeInteger(now)||now<0)return report('RECOVERY_UNAVAILABLE','RECOVERY_INVALID_INSPECT_ARGS');if(!await verify())return report('RECOVERY_UNAVAILABLE','RECOVERY_CAPABILITY_DENIED');const {data,error}=await client.rpc('list_expired_terminal_delivery_claims',{p_limit:limit});if(error||!Array.isArray(data))return report('RECOVERY_UNAVAILABLE','RECOVERY_STORE_UNAVAILABLE');const claims=Object.freeze(data.map(claim).filter(Boolean));if(claims.length!==data.length)return report('RECOVERY_UNAVAILABLE','RECOVERY_DATA_INTEGRITY_FAILED');const expiries=claims.map(row=>Date.parse(row.leaseExpiresAt));if(expiries.some(value=>value>now))return report('RECOVERY_UNAVAILABLE','RECOVERY_CLOCK_SKEW');const oldestAgeMs=claims.length?Math.floor(now-Math.min(...expiries)):0;return oldestAgeMs>slaMs?report('RECOVERY_SLA_BREACHED','EXPIRED_CLAIM_SLA_EXCEEDED',claims,oldestAgeMs):report('RECOVERY_HEALTHY',null,claims,oldestAgeMs);},
    async quarantine(input={}){const claimKey=String(input.claimKey||''),reasonCode=String(input.reasonCode||'');if(!KEY.test(claimKey)||!REASONS.has(reasonCode)||!await verify())return false;const {data,error}=await client.rpc('quarantine_expired_terminal_delivery_claim',{p_claim_key:claimKey,p_actor_hash:actorHash,p_reason_code:reasonCode});return !error&&data===true;}
  });
}
module.exports=Object.freeze({RECOVERY_VERSION,create});
