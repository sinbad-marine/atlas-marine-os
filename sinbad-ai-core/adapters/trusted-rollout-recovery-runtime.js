'use strict';
const journalModule=require('./supabase-rollout-activation-journal.js');
const storeModule=require('./supabase-rollout-recovery-authorization-audit.js');
const auditModule=require('./trusted-rollout-recovery-authorization-audit.js');
const verifierModule=require('./supabase-rollout-recovery-authorization-audit-verifier.js');
const readinessModule=require('./rollout-recovery-authorization-audit-readiness.js');
const authorizationModule=require('./trusted-terminal-rollout-recovery-authorization.js');

const RUNTIME_VERSION='sinbad-trusted-rollout-recovery-runtime/3V-v1';
const DATABASE_FINGERPRINT='sinbad-rollout-recovery-db/3U-20260820-v1';
const EXPECTED=Object.freeze({
  journal:'sinbad-supabase-rollout-activation-journal/3J-v1',
  auditEvent:'sinbad-trusted-rollout-recovery-authorization-audit/3N-v1',
  verifier:'sinbad-supabase-rollout-recovery-authorization-audit-verifier/3P-v1',
  readiness:'sinbad-rollout-recovery-authorization-audit-readiness/3Q-v1',
  authorization:'sinbad-trusted-terminal-rollout-recovery-authorization/3R-v1'
});
if(journalModule.JOURNAL_VERSION!==EXPECTED.journal||storeModule.AUDIT_VERSION!==EXPECTED.auditEvent||auditModule.AUDIT_VERSION!==EXPECTED.auditEvent||verifierModule.VERIFIER_VERSION!==EXPECTED.verifier||readinessModule.READINESS_VERSION!==EXPECTED.readiness||authorizationModule.AUTHORIZATION_VERSION!==EXPECTED.authorization)throw new Error('Unsupported trusted rollout recovery runtime chain');
const HEALTH_FIELDS=Object.freeze(['version','status','reasonCode','eventCount','pageCount','watermarkId']);
function healthSnapshot(value){if(!value||typeof value!=='object')return null;const output=Object.create(null);for(const name of HEALTH_FIELDS){let descriptor;try{descriptor=Object.getOwnPropertyDescriptor(value,name);}catch{return null;}if(!descriptor||!Object.hasOwn(descriptor,'value'))return null;output[name]=descriptor.value;}if(output.version!==EXPECTED.readiness||!['AUTHORIZATION_AUDIT_READINESS_READY','AUTHORIZATION_AUDIT_READINESS_BLOCKED'].includes(output.status)||(output.reasonCode!==null&&typeof output.reasonCode!=='string'))return null;for(const name of ['eventCount','pageCount'])if(output[name]!==null&&(!Number.isSafeInteger(output[name])||output[name]<0))return null;if(output.watermarkId!==null&&(!Number.isSafeInteger(output.watermarkId)||output.watermarkId<1))return null;if(output.status==='AUTHORIZATION_AUDIT_READINESS_READY'&&(output.reasonCode!==null||!Number.isSafeInteger(output.eventCount)||!Number.isSafeInteger(output.pageCount)||output.pageCount<1||((output.eventCount===0)!==(output.watermarkId===null))))return null;if(output.status==='AUTHORIZATION_AUDIT_READINESS_BLOCKED'&&output.reasonCode===null)return null;return Object.freeze(output);}
function policy(options){if(!options||typeof options!=='object')return null;const output=Object.create(null);for(const name of ['client','serviceRole','authorize','resolve','now','auditPageSize','auditMaxEvents']){let descriptor;try{descriptor=Object.getOwnPropertyDescriptor(options,name);}catch{return null;}if(!descriptor||!Object.hasOwn(descriptor,'value'))return null;output[name]=descriptor.value;}if(!output.client||typeof output.client!=='object'||output.serviceRole!==true||typeof output.authorize!=='function'||typeof output.resolve!=='function'||typeof output.now!=='function'||!Number.isSafeInteger(output.auditPageSize)||output.auditPageSize<1||output.auditPageSize>500||!Number.isSafeInteger(output.auditMaxEvents)||output.auditMaxEvents<output.auditPageSize||output.auditMaxEvents>100000)return null;let rpc;try{rpc=Object.getOwnPropertyDescriptor(output.client,'rpc');}catch{return null;}if(!rpc||!Object.hasOwn(rpc,'value')||typeof rpc.value!=='function')return null;output.client=Object.freeze({rpc:rpc.value.bind(output.client)});return Object.freeze(output);}

function create(options={}){
  const admitted=policy(options);if(!admitted){let clientDescriptor;try{clientDescriptor=options&&typeof options==='object'?Object.getOwnPropertyDescriptor(options,'client'):null;}catch{}if(!clientDescriptor)throw new TypeError('A trusted Supabase service-role client is required');throw new TypeError('Trusted functions and bounded audit readiness scan policy are required');}
  const pageSize=admitted.auditPageSize,maxEvents=admitted.auditMaxEvents;
  const base=Object.freeze({client:admitted.client,serviceRole:true,actorHash:options.actorHash,recoveryPurpose:options.recoveryPurpose,authorizationTtlMs:options.authorizationTtlMs,authorizationTimeoutMs:options.authorizationTimeoutMs,now:admitted.now,authorize:admitted.authorize,resolve:admitted.resolve,recoveryTimeoutMs:options.recoveryTimeoutMs});
  const journal=journalModule.create(base),store=storeModule.create(base),audit=auditModule.create({durable:true,append:event=>store.append(event)}),verifier=verifierModule.create(base),readiness=readinessModule.create({auditVerifier:verifier,pageSize,maxEvents}),authorization=authorizationModule.create({...base,activationJournal:journal,authorizationAudit:audit,auditReadiness:readiness});
  let verified=false,pending=null;
  async function preflight(){if(verified)return true;if(pending)return pending;pending=(async()=>{try{const {data,error}=await admitted.client.rpc('verify_rollout_recovery_runtime_access',{});return !error&&data===DATABASE_FINGERPRINT;}catch{return false;}})();const value=await pending;pending=null;if(value)verified=true;return value;}
  async function healthCheck(){if(!await preflight())return Object.freeze({version:RUNTIME_VERSION,status:'ROLLOUT_RECOVERY_RUNTIME_BLOCKED',reasonCode:'RUNTIME_PREFLIGHT_REQUIRED',eventCount:null,pageCount:null,watermarkId:null});let state;try{state=healthSnapshot(await readiness.check());}catch{return Object.freeze({version:RUNTIME_VERSION,status:'ROLLOUT_RECOVERY_RUNTIME_BLOCKED',reasonCode:'AUDIT_READINESS_EXCEPTION',eventCount:null,pageCount:null,watermarkId:null});}const ready=state?.status==='AUTHORIZATION_AUDIT_READINESS_READY';return Object.freeze({version:RUNTIME_VERSION,status:ready?'ROLLOUT_RECOVERY_RUNTIME_READY':'ROLLOUT_RECOVERY_RUNTIME_BLOCKED',reasonCode:ready?null:state?.reasonCode||'AUDIT_READINESS_NOT_READY',eventCount:state?.eventCount??null,pageCount:state?.pageCount??null,watermarkId:state?.watermarkId??null});}
  return Object.freeze({version:RUNTIME_VERSION,healthCheck,async issue(value){if(!await preflight())return Object.freeze({version:EXPECTED.authorization,status:'ROLLOUT_RECOVERY_AUTHORIZATION_BLOCKED',reasonCode:'RUNTIME_PREFLIGHT_REQUIRED',authorizationHash:null,issuedAt:null,expiresAt:null});return authorization.issue(value);},async recover(value){if(!await preflight())return Object.freeze({version:'sinbad-trusted-terminal-rollout-recovery/3L-v1',status:'ROLLOUT_RECOVERY_BLOCKED',reasonCode:'RUNTIME_PREFLIGHT_REQUIRED',attestationHash:null});return authorization.recover(value);}});
}
module.exports=Object.freeze({RUNTIME_VERSION,DATABASE_FINGERPRINT,EXPECTED,create});
