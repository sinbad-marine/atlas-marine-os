'use strict';
const REGISTRY_VERSION='sinbad-marine-capability-registry/2-v1';
const capabilities=Object.freeze({SENSOR_TRUST:Object.freeze({version:'sinbad-sensor-trust-capability/1-v1',state:'CONTRACT_ONLY'}),METOCEAN:Object.freeze({version:'sinbad-metocean-capability/1-v1',state:'CONTRACT_ONLY'}),CELESTIAL:Object.freeze({version:'sinbad-celestial-capability/1-v1',state:'CONTRACT_ONLY'}),PNT:Object.freeze({version:'sinbad-pnt-capability/1-v1',state:'CONTRACT_ONLY'})});
const operations=new Set(['CONTRACT_VALIDATION','LIVE_SENSOR','NAVIGATION_SOLUTION','CONTROL']);
function evaluate(request={}){let c,o,names,symbols;try{if(!request||typeof request!=='object'||Array.isArray(request))throw new Error('invalid');names=Object.getOwnPropertyNames(request);symbols=Object.getOwnPropertySymbols(request);if(symbols.length||names.length!==2||!names.includes('capability')||!names.includes('operation'))throw new Error('invalid');const cd=Object.getOwnPropertyDescriptor(request,'capability'),od=Object.getOwnPropertyDescriptor(request,'operation');c=cd&&Object.hasOwn(cd,'value')?cd.value:null;o=od&&Object.hasOwn(od,'value')?od.value:null;}catch{c=null;o=null;}if(!Object.hasOwn(capabilities,c)||!operations.has(o))return Object.freeze({version:REGISTRY_VERSION,status:'CAPABILITY_BLOCKED',reasonCode:'CAPABILITY_REQUEST_INVALID',capability:null,operation:null});if(o==='CONTRACT_VALIDATION')return Object.freeze({version:REGISTRY_VERSION,status:'CAPABILITY_CONTRACT_AVAILABLE',reasonCode:null,capability:c,operation:o});return Object.freeze({version:REGISTRY_VERSION,status:'CAPABILITY_BLOCKED',reasonCode:o==='LIVE_SENSOR'?'PHYSICAL_SENSOR_ADAPTER_NOT_AUTHORIZED':o==='NAVIGATION_SOLUTION'?'PNT_SOLUTION_NOT_AUTHORIZED':'SAFETY_CRITICAL_CONTROL_NOT_AUTHORIZED',capability:c,operation:o});}
module.exports=Object.freeze({REGISTRY_VERSION,capabilities,evaluate});
