const test=require('node:test');
const assert=require('node:assert/strict');
const manifest=require('../engines/studio/studio-pro-03-acceptance-manifest.js');
const planner=require('../engines/studio/model-proposal-diff-planner.js');
const writer=require('../engines/studio/model-proposal-revision-writer.js');
test('freezes the finite Pro 0.3.1 no-delete acceptance scope',()=>{assert.equal(manifest.version,'0.3.1');assert.equal(manifest.status,'PRO_STUDIO_REVIEW_AND_REVISION_FOUNDATION_ACCEPTED');assert.equal(Object.isFrozen(manifest),true);assert.equal(Object.isFrozen(manifest.historicalExecutionBlocker),true);for(const item of ['READ_ONLY_MODEL_PROPOSAL_DIFF','DELETION_ALWAYS_DENIED','ATOMIC_DERIVED_REVISION_CREATION','ORIGINAL_WORKSPACE_BYTE_PRESERVATION'])assert.ok(manifest.acceptedCapabilities.includes(item),item);});
test('preserves the historical blocker and points to its narrowly scoped resolution',()=>{assert.equal(manifest.historicalExecutionBlocker.code,'NO_NETWORK_ISOLATING_CODE_SANDBOX');assert.equal(manifest.historicalExecutionBlocker.resolvedForExactVerifiedSoftwareTestsBy,'PRO_STUDIO_DOCKER_TEST_EVIDENCE_ACCEPTED@0.4.4');assert.ok(manifest.historicalExecutionBlocker.stillBlocked.includes('MODEL_INFERENCE_ISOLATION'));for(const item of ['GENERATED_CODE_EXECUTION','GENERATED_RUNTIME_TESTS','ORIGINAL_WORKSPACE_MUTATION','FILE_DELETION','AUTOMATIC_MERGE','LIVE_DEPLOYMENT_OR_PUBLISH'])assert.ok(manifest.prohibitedCapabilities.includes(item),item);});
test('accepted review and revision APIs expose no prohibited capability',()=>{for(const api of [planner.create(),writer.create()])for(const field of ['run','execute','testCode','delete','remove','overwriteOriginal','mergeOriginal','writeCore','fetch','publish','deploy'])assert.equal(field in api,false,field);});
