'use strict';
const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');const path=require('node:path');
test('adapter-facing production modules cannot import terminal internals',()=>{const root=path.resolve(__dirname,'../adapters'),allowed=new Set(['trusted-terminal-delivery-adapter.js','terminal-state-transition.js']),forbidden=/require\(['"]\.\.\/(?:delivery\/(?:terminal-delivery-receipt|single-use-terminal-closure|terminal-completion-gate)|verification\/(?:terminal-delivery-receipt-verifier|terminal-closure-verifier|terminal-delivery-audit-verifier)|audit\/terminal-delivery-audit-record)\.js['"]\)/u;for(const name of fs.readdirSync(root)){if(!name.endsWith('.js')||allowed.has(name))continue;assert.doesNotMatch(fs.readFileSync(path.join(root,name),'utf8'),forbidden,`${name} bypasses the Phase 2X package boundary`);}});
