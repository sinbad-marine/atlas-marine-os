'use strict';
const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');const path=require('node:path');const runtime=require('../adapters/trusted-rollout-recovery-runtime.js');
test('migration and runtime share one exact deployment fingerprint',()=>{const sql=fs.readFileSync(path.resolve(__dirname,'../../supabase/migrations/20260820_rollout_recovery_runtime_fingerprint.sql'),'utf8');assert.match(runtime.DATABASE_FINGERPRINT,/^sinbad-rollout-recovery-db\/3U-20260820-v1$/u);assert.ok(sql.includes(`then '${runtime.DATABASE_FINGERPRINT}'`));for(const pattern of [/drop function if exists public\.verify_rollout_recovery_runtime_access/iu,/returns text/iu,/auth\.role\(\)='service_role'/iu,/revoke all on function/iu,/grant execute on function/iu,/commit;/iu])assert.match(sql,pattern);});
